import 'package:flutter/material.dart';

class TrainingsTab extends StatelessWidget {
  const TrainingsTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Trainings coming soon'),
    );
  }
}
